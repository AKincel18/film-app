package com.filmapp.generic;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestBody;

import java.util.List;

/*
public interface GenericController<Entity extends BaseEntity> {
    List<Entity> getAll();
    ResponseEntity<Entity> create(@RequestBody Entity entityToCreate);
    ResponseEntity<Entity> update(@RequestBody Entity entityToUpdate);
    ResponseEntity<List<Entity>> delete(@PathVariable Long id);
}
*/
